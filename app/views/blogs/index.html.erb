<p id="notice"><%= notice %></p>

<div class="container">

  <div class="row">

    <div class="col-sm-8 blog-main">
	<%= paginate @blogs %>

    <%= render @blogs %>

    </div>
    <%= render partial: "shared/blogsidebar", locals: {topics: @topics} %>

  </div>
  <%= paginate @blogs %>
</div>


<%= link_to 'New Blog', new_blog_path, class: "button primary" if logged_in?(:site_admin) %>
