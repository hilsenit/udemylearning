<tr>
  <td><%= blog.title.upcase %></td>
  <td>Published: <%= distance_of_time_in_words(blog.created_at, Time.now) %></td>
  <td><%= blog.body %></td>
  <td><%= blog.published? ? "Udgivet" : "Kladde" if logged_in?(:site_admin) %></td>
  <td class="button secondary"><%= link_to('Ændre status', toggle_status_path(blog)) if logged_in?(:site_admin) %></td>
  <td class="button secondary"><%= link_to('Show', blog) if logged_in?(:site_admin) %></td>
  <td class="button secondary"><%= link_to('Edit', edit_blog_path(blog)) if logged_in?(:site_admin) %></td>
  <td class="button secondary"><%= link_to('Delete post', blog, method: :delete, data: { confirm: 'Are you sure?' }) if logged_in?(:site_admin) %></td>
</tr>
