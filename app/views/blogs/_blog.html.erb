<tr>
  <td><%= blog.title.upcase %></td>
  <td><%= blog.body %></td>
  <td><%= blog.published? ? "Udgivet" : "Kladde" %></td>
  <td class="button secondary"><%= link_to 'Ændre status', toggle_status_path(blog) %></td>
  <td class="button secondary"><%= link_to 'Show', blog %></td>
  <td class="button secondary"><%= link_to 'Edit', edit_blog_path(blog) %></td>
  <td class="button secondary"><%= link_to 'Delete post', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
